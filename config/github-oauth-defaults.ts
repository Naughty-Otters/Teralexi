/**
 * Optional bundled GitHub OAuth App credentials for teralexi.
 * Users should register an OAuth App with callback URL `http://127.0.0.1:7780`
 * and override via `app.github.clientId` / `app.github.clientSecret` in
 * `~/.teralexi/config/config.properties` or `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET`.
 */
export const BUNDLED_GITHUB_OAUTH_CLIENT_ID = ''

export const BUNDLED_GITHUB_OAUTH_CLIENT_SECRET = ''
