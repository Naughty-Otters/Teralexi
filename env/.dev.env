# Open-source default: talk to the production Teralexi platform.
# For a local backend, copy env/.dev.local.env.example → env/.dev.local.env
# (gitignored) or set BASE_API=... when starting the app.
BASE_API = 'https://api.teralexi.com/'
ENTITLEMENT_SIGNING_PUBLIC_KEY_PEM = "-----BEGIN PUBLIC KEY-----\nMCowBQYDK2VwAyEAdYcRhP9/k22MDadKUCklatZbiG0XMtM55YrAjRRYO5I=\n-----END PUBLIC KEY-----"
# Enable update checks in npm run dev (maps to app.desktop.forceDevUpdateConfig).
DESKTOP_UPDATE_FORCE_DEV = 'true'
NODE_ENV = 'development'
