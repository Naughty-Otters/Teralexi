# OpenFDE platform backend (GraphQL, auth, support upload, renderer API calls).
BASE_API = 'http://localhost:8000'
# Enable update checks in npm run dev (maps to app.desktop.forceDevUpdateConfig).
DESKTOP_UPDATE_FORCE_DEV = 'true'
NODE_ENV = 'development'
