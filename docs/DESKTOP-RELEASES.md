# Desktop releases (S3 + public update feed)

Production desktop updates use a **private S3 bucket** for CI uploads and a **public HTTP feed** on your platform API (`BASE_API`). The GitHub repository stays private; end users never receive publish credentials. **Updates do not require sign-in** (unlike support upload).

## Architecture

```
GitHub Actions (Release workflow)
  AWS credentials (GitHub secrets, write-only)
       │
       ▼
  s3://<bucket>/desktop/releases/stable/
       ├── latest-mac.yml
       ├── latest.yml
       └── installers…

Installed OpenFDE app (no auth)
  GET {BASE_API}/desktop/releases/stable/latest-mac.yml
  GET {BASE_API}/desktop/releases/stable/<installer>
       │
       ▼
  Your API or CDN serves objects from S3 (public read on this prefix)
```

## S3 layout

| Key | Purpose |
| --- | --- |
| `desktop/releases/stable/latest.yml` | Windows feed (`electron-updater`) |
| `desktop/releases/stable/latest-mac.yml` | macOS feed |
| `desktop/releases/stable/latest-linux.yml` | Linux feed (when built) |
| `desktop/releases/stable/*.{exe,zip,dmg,blockmap}` | Installers referenced by feed |

The `desktop/releases/stable/` prefix must be **publicly readable** (via CloudFront, API static proxy, or bucket policy on that prefix only). Keep the rest of the bucket private.

Optional staging channel: `desktop/releases/sit/` — override `app.desktop.releasesUrl` in `env/.sit.env`.

## GitHub Actions secrets

Configure in the repository **Settings → Secrets**:

| Secret | Purpose |
| --- | --- |
| `AWS_ACCESS_KEY_ID` | CI upload (S3 `PutObject` on release prefix only) |
| `AWS_SECRET_ACCESS_KEY` | CI upload |
| `AWS_REGION` | Bucket region |
| `S3_RELEASE_BUCKET` | Bucket name |

The Release workflow runs `npm run release:upload-s3` after building installers.

Local upload (after a build):

```bash
export AWS_ACCESS_KEY_ID=...
export AWS_SECRET_ACCESS_KEY=...
export AWS_REGION=us-east-1
export S3_RELEASE_BUCKET=your-bucket
npm run release:mac    # or release:win
npm run release:upload-s3
```

## Client configuration

In `env/.prod.env` (maps to `app.base.apiUrl`):

```properties
BASE_API = 'https://api.example.com'
```

Default update feed: `{BASE_API}/desktop/releases/stable/`

Optional override in `config.properties`:

```properties
app.desktop.releasesUrl=desktop/releases/stable
```

The desktop app calls `autoUpdater.setFeedURL({ provider: 'generic', url: feedUrl })` at runtime with **no** `Authorization` header.

## Server API contract (implement on your backend)

Expose anonymous `GET` for the stable release prefix. Typical options:

1. **Reverse proxy / static route** — nginx or your API serves `GET /desktop/releases/stable/*` from S3 or CloudFront.
2. **CloudFront** — origin = S3 prefix; URL = `{BASE_API}/desktop/releases/stable/` (via custom domain or path routing).

### Feed metadata

```
GET /desktop/releases/stable/latest-mac.yml
GET /desktop/releases/stable/latest.yml
GET /desktop/releases/stable/latest-linux.yml
```

Return YAML with `Content-Type: application/x-yaml`.

Example feed (generated by electron-builder — do not hand-edit):

```yaml
version: 0.0.2
files:
  - url: OpenFDE-0.0.2-mac.zip
    sha512: <base64-sha512>
    size: 198765432
path: OpenFDE-0.0.2-mac.zip
sha512: <base64-sha512>
releaseDate: '2026-06-28T10:00:00.000Z'
```

`sha512` and `size` are required — empty values cause `electron-updater` to fail. For a local test stub:

```yaml
version: 0.0.2-test
files:
  - url: OpenFDE-0.0.2-test-mac.zip
    sha512: NQxPQ+Mv0cT5oOwR1rX8k4H4X6TqQSl6an99h5xH1cckgrWYVW7UavzKRQ8nu49X1duwxjqAnCLl3i3mtPQqbw==
    size: 156
path: OpenFDE-0.0.2-test-mac.zip
sha512: NQxPQ+Mv0cT5oOwR1rX8k4H4X6TqQSl6an99h5xH1cckgrWYVW7UavzKRQ8nu49X1duwxjqAnCLl3i3mtPQqbw==
releaseDate: '2026-06-28T10:00:00.000Z'
```

`url` paths are relative to the feed base URL.

### Installer binaries

```
GET /desktop/releases/stable/<filename>
```

Serve the file bytes (or redirect to a public CloudFront URL).

## IAM sketch

**CI user (publish only):**

```json
{
  "Effect": "Allow",
  "Action": ["s3:PutObject"],
  "Resource": "arn:aws:s3:::your-bucket/desktop/releases/*"
}
```

**CDN / API (read only on release prefix):**

```json
{
  "Effect": "Allow",
  "Action": ["s3:GetObject"],
  "Resource": "arn:aws:s3:::your-bucket/desktop/releases/*"
}
```

Never embed CI credentials in the desktop app.

## Related docs

- [RELEASE.md](./RELEASE.md) — version bumps and Release workflow
- [BUILD-AND-RELEASE.md](../BUILD-AND-RELEASE.md) — build environments
