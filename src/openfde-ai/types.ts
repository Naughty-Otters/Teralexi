/**
 * AI SDK types — re-exported for app code. Import from `@openfde-ai`, not `ai`.
 */
export type {
  ChatTransport,
  FinishReason,
  IdGenerator,
  LanguageModelUsage,
  ModelMessage,
  TextPart,
  TextUIPart,
  UIMessage,
  UIMessageChunk,
  UIMessagePart,
  UITools,
} from 'ai'
