/**
 * Agent reference resources — docs, scripts, and path resolution.
 *
 * Import from this barrel only. Agent steps should use {@link ReferenceContext} via
 * {@link AgentFlowContext.references}, not this module directly.
 */

export { ReferenceContext } from './context'
